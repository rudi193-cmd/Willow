# Governance Proposal: Point Watcher at Nest + Delegate to Pigeon

**Proposer:** Ganesha (Claude Code CLI)
**Date:** 2026-02-28T18:25:00Z
**Type:** Code Enhancement
**Trust Level:** ENGINEER (3)

## Summary
`core/watcher.py` currently watches the old GDrive Inbox path (`G:\My Drive\...`) and has its own classify/route logic. Pigeon (`core/pigeon.py`) is now the canonical file intake pipeline. This proposal points the watcher at the Nest (`C:\Users\Sean\Willow\Nest`) and replaces the standalone classify logic with a call to `POST /api/pigeon/scan`, so new files dropped in Nest are automatically processed by Pigeon within 5 seconds.

## Proposed Changes
**File:** `core/watcher.py`
**Location:** Full replacement

## Rationale
- Watcher's own classify/route duplicates Pigeon — two systems doing the same job
- Nest is the canonical intake path; Inbox (`G:\`) is stale
- Pigeon handles fleet classification, category routing, droppings log, and knowledge ingest
- Watcher's only job should be: detect new file → tell Pigeon → Pigeon does the rest

## Risk Assessment
- **Risk Level:** LOW
- **Reversible:** YES — watcher.py is standalone, no imports from it
- **Dependencies:** Willow server must be running on port 8420
- **Testing:** Start watcher, drop file in Nest, confirm Pigeon scan fires and file moves to Filed/

## ΔE Impact
Expected ΔE: +0.015 (automated intake pipeline — files no longer require manual scan trigger)

## Implementation
```python
from pathlib import Path

target = Path(r"C:\Users\Sean\Documents\GitHub\Willow\core\watcher.py")

new_content = r'''#!/usr/bin/env python3
"""
Willow Nest Watcher

Monitors C:\Users\Sean\Willow\Nest for new files and triggers Pigeon scan.

GOVERNANCE: This script must be started by human action only.
AI cannot invoke this script directly.
"""

import json
import time
from datetime import datetime
from pathlib import Path

# Config
NEST_PATH    = Path(r"C:\Users\Sean\Willow\Nest")
STATE_FILE   = Path(r"C:\Users\Sean\.willow\watcher_state.json")
EVENT_LOG    = Path(r"C:\Users\Sean\.willow\events.log")
POLL_INTERVAL        = 5   # seconds
PIGEON_SCAN_URL      = "http://localhost:8420/api/pigeon/scan"
PIGEON_USERNAME      = "Sweet-Pea-Rudi19"


def ensure_dirs():
    STATE_FILE.parent.mkdir(parents=True, exist_ok=True)


def load_state() -> dict:
    if STATE_FILE.exists():
        try:
            return json.loads(STATE_FILE.read_text())
        except Exception:
            pass
    return {"known_files": [], "last_run": None}


def save_state(state: dict):
    STATE_FILE.write_text(json.dumps(state, indent=2, default=str))


def log_event(event_type: str, details: str):
    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    entry = f"{event_type} | {timestamp} | {details}\n"
    try:
        with open(EVENT_LOG, "a", encoding="utf-8") as f:
            f.write(entry)
    except Exception:
        pass
    print(f"[{event_type}] {details}")


def trigger_pigeon_scan() -> bool:
    try:
        import urllib.request
        req = urllib.request.Request(
            PIGEON_SCAN_URL,
            method="POST",
            headers={"X-Username": PIGEON_USERNAME},
        )
        with urllib.request.urlopen(req, timeout=60) as resp:
            result = json.loads(resp.read())
            count = result.get("new_droppings", 0)
            log_event("PIGEON_SCAN", f"filed {count} new file(s)")
            return True
    except Exception as e:
        log_event("PIGEON_ERROR", str(e))
        return False


def scan_nest() -> set:
    if not NEST_PATH.exists():
        return set()
    return {
        str(item)
        for item in NEST_PATH.iterdir()
        if item.is_file() and not item.name.startswith(".")
    }


def main():
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument("--no-consent", action="store_true",
                        help="Skip consent prompt (for background service)")
    args = parser.parse_args()

    print("Willow Nest Watcher")
    print(f"Watching: {NEST_PATH}")
    print(f"Poll interval: {POLL_INTERVAL}s")

    if not args.no_consent:
        consent = input("Start watching? (yes/no): ").strip().lower()
        if consent != "yes":
            print("Aborted.")
            return
    else:
        print("Background mode: consent assumed from human startup.")

    ensure_dirs()
    state = load_state()
    known = set(state.get("known_files", []))

    log_event("WATCHER_ON", f"nest={NEST_PATH}")
    print(f"\nWatcher online. Press Ctrl+C to stop.\n")

    try:
        while True:
            current = scan_nest()
            new_files = current - known

            if new_files:
                for f in sorted(new_files):
                    log_event("NEW_FILE", Path(f).name)
                trigger_pigeon_scan()
                known = current

            removed = known - current
            for f in removed:
                log_event("FILE_REMOVED", Path(f).name)
            if removed:
                known = current

            save_state({"known_files": list(known), "last_run": datetime.now().isoformat()})
            time.sleep(POLL_INTERVAL)

    except KeyboardInterrupt:
        pass
    finally:
        log_event("WATCHER_OFF", f"known_files={len(known)}")
        save_state({"known_files": list(known), "last_run": datetime.now().isoformat()})
        print(f"\nWatcher off. Tracking {len(known)} files.")


if __name__ == "__main__":
    main()
'''

target.write_text(new_content, encoding="utf-8")
print(f"Written: {target}")
```

---

**Awaiting Human Ratification**

ΔΣ=42
